[project]
name = "etools"
version = "0.2.0"
description = "ETools — Embedded engineer workbench (MCU flash + serial / network / SSH tools)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "ETools Contributors" }]
dependencies = [
    "PySide6>=6.5",
    "pyocd>=0.40",
    "pyserial>=3.5",
    "paramiko>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "ruff>=0.4",
]
packaging = [
    "pyinstaller>=6.0",
]

[project.scripts]
etools = "etools.app:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["etools*"]

[tool.setuptools.package-data]
"etools.ui.forms" = ["*.ui"]
"etools.ui" = ["forms/*.ui", "resources/icons/*.svg"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint.isort]
known-first-party = ["etools"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
filterwarnings = [
    "ignore::DeprecationWarning",
]
